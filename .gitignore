# OS / editor cruft
.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Secrets & credentials
.env
.env.*
*.env
*.pem
*.key
*.p12
*.pfx

# Build output — the Pages workflow generates data.json at build time; the
# published copy lives only in the deploy artifact, never in the repo.
data.json
